<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Productos</title>
</head>
<body>
  
  <ul>
    {{#each products}}
      <li>
        
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <p>Price: ${{this.price}}</p>
        <a href="/productsDetail/{{this._id}}">Ver detalles</a>
        <a href="/cart/add/{{this._id}}">Agregar al carrito</a>
      </li>
    {{/each}}
  </ul>
  <div>
    {{#if hasPrevPage}}
      <a href="/products?limit={{limit}}&page={{prevPage}}&sort={{sort}}&query={{query}}">Anterior</a>
    {{/if}}
    {{#if hasNextPage}}
      <a href="/products?limit={{limit}}&page={{nextPage}}&sort={{sort}}&query={{query}}">Siguiente</a>
    {{/if}}
  </div>
</body>
</html>