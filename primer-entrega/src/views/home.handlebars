<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{Home}}</title>
</head>
<body>
    <h1>{{Lista de productos}}</h1>
    <ul id="product-list">
        {{#each products}}
            <li>{{title}} - ${{price}}</li>
        {{/each}}
    </ul>
</body>
</html>

